import { TrustedURLServicePipe } from './trusted-urlservice.pipe';

describe('TrustedURLServicePipe', () => {
  it('create an instance', () => {
    const pipe = new TrustedURLServicePipe();
    expect(pipe).toBeTruthy();
  });
});
